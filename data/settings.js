const botsMin = 5
const botsMax = 11

const scoreLimits = [150, 200, 300]

const health = [
    "Normal (100%)",
    "Hard (75%)",
    "Extreme (50%)",
    "Double Health (200%)"
]

const veteran = ["Sì", "No"]

const timeSpawn = [5, 7, 7.5 , 10, 15, 30]
const healthRecovery = ["Slow", "Fast", "Normal"]

export {botsMin, botsMax, health, scoreLimits, veteran, timeSpawn, healthRecovery}